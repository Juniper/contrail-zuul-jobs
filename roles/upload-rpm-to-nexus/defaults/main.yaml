---
local_repo_path: '{{ zuul.executor.work_root }}/packages'
nexus_creds_path: '{{ zuul.executor.work_root }}/nexus-creds'
nexus_login: '{{ nexus_credentials.user }}'
nexus_password: '{{ nexus_credentials.password }}'
