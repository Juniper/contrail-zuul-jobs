---
- name: Load variables from results.json
  include_vars:
    file: "{{ zuul.executor.work_root }}/results.json"
  delegate_to: localhost

- debug:
    msg: "vars: {{ zuul.executor.work_root }}/results.json"

- debug:
    msg: "file content: {{lookup('file', '{{ zuul.executor.work_root }}/results.json') }}"