---
third_party_pkg_facts: "{{ zuul.projects | selectattr('short_name', 'equalto', 'contrail-third-party-packages') }}"
rpm_dir: "{{ third_party_pkg_facts | map(attribute='src_dir') | list | first }}"

