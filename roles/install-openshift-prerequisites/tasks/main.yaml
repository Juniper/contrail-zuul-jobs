---

- name: include OS-specific tasks
  include: '{{ item }}'
  with_first_found:
    - '{{ ansible_distribution | lower }}_{{ openshift_version }}.yaml'
    - '{{ ansible_os_family | lower }}_{{ openshift_version }}.yaml'

