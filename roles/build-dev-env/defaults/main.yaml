---
image_tag: "build"
image_name: "opencontrailnightly/developer-sandbox"
sandbox_branch: "{{ zuul.branch }}"
src_dir_json_query: "[?short_name=='contrail-dev-env'].src_dir"
workdir: '{{ zuul.projects | json_query(src_dir_json_query) | first }}'

