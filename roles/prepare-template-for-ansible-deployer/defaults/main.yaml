---
docker_provision_dir: '{{ ansible_env.HOME }}/src/review.opencontrail.org/Juniper/contrail-ansible-deployer'
contrail_version: 'queens-master-latest'
openstack_version: 'ocata'

common_control_plane_roles:
  config_database:
  config:
  control:
  analytics_database:
  analytics:
  webui:

control_plane_specific_roles:
  openstack:
    openstack:
  kubernetes:
    k8s_master:
    kubemanager:

data_plane_specific_roles:
  openstack:
    openstack_compute:
    vrouter:
  kubernetes:
    k8s_node:
