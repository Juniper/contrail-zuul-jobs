---
- name: Prepare packaging variables
  contrail_packaging:
    zuul: "{{ zuul }}"
    release_type: "{{ release_type }}"
    build_number: "{{ build_number | default('unset_build_number') }}"

# Needed while centos74 is not supported correctly
- name: Create missing directory
  file:
    state: directory
    path: "{{ ansible_env.HOME }}/{{ packaging.target_dir }}/controller/build/"

- name: Create links for centos 74 specs
  file:
    path: "{{ ansible_env.HOME }}/{{ packaging.target_dir }}/{{ item.path }}"
    src: "{{ ansible_env.HOME }}/{{ packaging.target_dir }}/{{ item.source }}"
    state: link
  with_items:
    - path: "controller/build/package_configs"
      source: "tools/packaging/build/package_configs"
    - path: "tools/packaging/build/package_configs/centoslinux741708/mitaka"
      source: "tools/packaging/build/package_configs/centoslinux741708/ocata"


