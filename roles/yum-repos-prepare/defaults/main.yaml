---
openstack_version: ocata
cloud_orchestrator: 'openstack'
openshift_version: '3.9'
tpc_repo_location: >-
  {%- if zuul.pipeline == 'post-merge' -%}
    source
  {%- else -%}
    binary
  {%- endif -%}
