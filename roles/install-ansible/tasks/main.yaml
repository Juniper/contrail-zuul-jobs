---

- name: install ansible
  become: true
  pip:
    name: 'ansible'
    version: '{{ ansible_ver }}'
    state: 'present'

- name: install python-netaddr
  become: true
  pip:
    name: 'netaddr'
    state: 'present'

- name: reinstall enum34
  pip:
    name: enum34
    version: "1.1.6"

