---
- name: install deploy-for-k8s script dependencies
  package:
    name: '{{ item }}'
    state: present
  become: true
  with_items:
    - unzip
    - wget

- name: run script
  command: /go/src/github.com/Juniper/contrail/tools/deploy-for_k8s.sh
