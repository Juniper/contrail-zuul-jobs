---

- name: delete contrail-tpc.repo
  file:
    state: absent
    path: '/etc/contrail-tpc.repo'
  become: true

- name: install python-bitstring
  yum:
    name: '{{ item }}'
    state: present
  with_items:
    - python-bitstring
  become: true
