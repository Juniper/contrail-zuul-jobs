---
- name: install required packages
  pip:
    requirements: 'requirements.txt'
    chdir: '{{ source_dir }}'
  become: True

- name: build HTML documentation
  command: make html
  args:
    chdir: '{{ source_dir }}'
