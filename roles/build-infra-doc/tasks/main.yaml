---
- name: install required packages
  pip:
    requirements: 'requirements.txt'
    chdir: '{{ workdir }}'
  become: True

- name: build HTML documentation
  command: make html
  args:
    chdir: '{{ workdir }}'

