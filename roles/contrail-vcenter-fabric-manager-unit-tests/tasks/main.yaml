---
- name: install requirements
  pip:
    requirements: "{{ zuul_project_dir }}/requirements.txt"
    virtualenv: "{{ zuul_project_dir }}/venv"
- name: run unit tests
  command: "pytest {{ zuul_project_dir }}/tests/unit"
  register: result
  failed_when: result.rc =! 0

