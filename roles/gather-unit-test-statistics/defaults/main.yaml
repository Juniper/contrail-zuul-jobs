---

script_dir: '{{ zuul.projects | selectattr("short_name", "equalto", "ci-utils") | map(attribute="src_dir") | list | first }}/tungsten_ci_utils/generate_build_change_info'
venv_dir: '{{ ansible_env.HOME }}/test_statistics_venv'
