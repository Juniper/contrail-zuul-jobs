---
container_name:
    - contrail-windows-cnm-plugin
    - contrail-windows-vrouter
src_dir_json_query: "[?short_name=='containerregistry'].src_dir"
workdir: '{{ zuul.projects | json_query(src_dir_json_query) | first }}'

