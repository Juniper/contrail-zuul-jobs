---
container_name:
    - contrail-windows-cnm-plugin
    - contrail-windows-vrouter
containerregistry_facts: "{{ zuul.projects | selectattr('short_name', 'equalto', 'containerregistry') }}"
workdir: '{{ containerregistry_facts | map(attribute="src_dir") | list | first }}'

