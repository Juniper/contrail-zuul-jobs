---

# 'with_items' isn't valid for playbook includes
- include: ./playbooks/testing_positive.yaml
  vars:
    rule_id: 'ANSIBLE-J0003'

- include: ./playbooks/testing_positive.yaml
  vars:
    rule_id: 'ANSIBLE-J0007'
