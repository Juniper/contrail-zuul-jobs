---

# with_items isn't valid for playbook includes, just for task includes.
- include: reusable_positive.yaml
    rule_id='ANSIBLE-J0003'
- include: reusable_positive.yaml
    rule_id='ANSIBLE-J0007'

