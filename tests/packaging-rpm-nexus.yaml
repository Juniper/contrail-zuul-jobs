---
- hosts: builder
  roles:
    - fetch-packages

- hosts: localhost
  roles:
    - role: contrail-common-libs
    - role: packaging-rpm-nexus
      vars:
        nexus_credentials.user: test
        nexus_credentials.password: test123
