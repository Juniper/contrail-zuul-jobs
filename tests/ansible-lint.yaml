---

- hosts: all
  become: true
  pre_tasks:
    - name: 'install test requirements'
      pip:
        name: '{{ item }}'
      with_items:
        - 'ansible-lint'
        - 'ruamel.yaml'

- include: ansible-lint-positive.yaml
- include: ansible-lint-negative.yaml

