---
- hosts: builder
  roles:
    - fetch-packages

- hosts: builder
  roles:
    - role: publish-rpm-nexus
      remote_repo_path: 'yum-tpc-test'
      nexus_credentials:
        user: 'test'
        password: 'test123'
