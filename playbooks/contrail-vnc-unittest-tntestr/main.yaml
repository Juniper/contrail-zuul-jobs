---
- hosts: all
  roles:
    - zuul-include-vars
    - contrail-tpc-repos
    - packaging-prepare-contrail2
    - contrail-unittests-tntestr
  post_tasks:
    - name: fail
      fail:
        msg: 'fail deliberately'
