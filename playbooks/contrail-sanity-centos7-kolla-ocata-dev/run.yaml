- hosts: all
  roles:
    - acquire-ip-address
    - self-ssh-key
    - role: contrail-ansible-deployer-new-dev
      cloud_orchestrator: openstack
      openstack_version: ocata
      kolla_docker_distro: "ubuntu"
      openstack_version: "ocata"
      kolla_version: "ocata"
      contrail_container_tag: "master-45"
    - sanitytest
