- hosts: all
  vars:
    cloud_orchestrator: openstack
    openstack_version: ocata
    kolla_docker_distro: ubuntu
    kolla_version: ocata
    contrail_container_tag: master-46
    contrail_version: master-46
    registry_port: "{{ docker_registry.port }}"
  roles:
    - acquire-ip-address
    - self-ssh-key
    - contrail-ansible-deployer-new-dev
    - prepare-template-for-sanity
    - sanitytest
