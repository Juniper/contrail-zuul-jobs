---
- hosts: all
  vars:
    source_location: "//s/vrouter/input"
  tasks:
    - name: Do the job
      command: echo Job done!
      register: the_message
    - name: Print the message
      debug:
        msg: "{{ the_message }}"
