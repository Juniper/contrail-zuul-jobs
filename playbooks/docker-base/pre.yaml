---
- hosts: all
  roles:
    - role: yum-repos-prepare
      tpc_location: binarys
    - install-docker-daemon
    - zuul-include-vars
    - role: add-insecure-registry
      new_registry: "{{ contrail_docker_registry }}"
    - add-registry-proxy
