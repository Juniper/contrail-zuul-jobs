- hosts: all
  roles:
    - load-docker-registry-info
    - acquire-ip-address
    - role: self-ssh-key
      user: root
    - contrail-helm-deployer
    - role: sanitytest
      openstack_version: ocata
