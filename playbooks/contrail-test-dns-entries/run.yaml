---

- hosts: ubuntu-node
  tasks:
    - command: "ping -c 5 {{ hostvars['centos-node'].ansible_fqdn }}"

- hosts: centos-node
  tasks:
    - command: "ping -c 5 {{ hostvars['ubuntu-node'].ansible_fqdn }}"

